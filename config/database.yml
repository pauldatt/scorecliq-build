default: &default
      adapter: postgresql
      encoding: unicode
      pool: 5
      username: kdhatt # same name for a superuser
      password: otohime # same password for the database owner. 
      host: <%= ENV['IP'] %> # config the hba_file and fix the template 1.
      # testing git ingore
  
development:
  <<: *default
  database: app_development

test:
  <<: *default
  database: app_test


production:
  <<: *default
  database: app_production
  
  
# The directory is part of gitignore