<%= form_for [@scoreboard, @schedule], remote: true do |f| %> 
         
 <%= render 'shared/error_messages', object: f.object %>
      
 <%= render 'schedules/schedulesform', :f => f %>
            
 <%= f.submit "update", :data => {:disable_with => "Updating..."}, class: " schedule-submit-button btn btn-primary" %>
                
<% end %>

