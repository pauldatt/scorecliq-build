<%= form_for [@scoreboard, @team] do |f| %> 
         
 <%= render 'shared/error_messages', object: f.object %>
      
 <%= render 'teams/teamsform', :f => f %>
            
 <%= f.submit "Add", class: " form-3 btn btn-primary" %>
                
<% end %>

<%= @team.id %>
